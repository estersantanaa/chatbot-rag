# Frontend (ClownorCloud)

Interface React do chatbot. A API precisa estar rodando em `http://localhost:8000`.

```powershell
cd frontend
copy .env.example .env
npm install
npm run dev
```

Abra http://localhost:5173
